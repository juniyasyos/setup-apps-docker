# Network Definition for RSCH Infrastructure
# Defines the shared network used by all services.

networks:
  default:
    name: rsch-apps_default
    driver: bridge
    ipam:
      config:
        - subnet: 172.25.0.0/16
